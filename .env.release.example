# Copy this file to .env.release for release deployment.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=pass
POSTGRES_DB=mcp_db

# Public bind port for FastAPI middleware endpoint.
MCP_API_PORT=8000
MCP_API_HOST=0.0.0.0

# Timeout for MCP server calls to middleware API (seconds).
MCP_API_TIMEOUT_SECONDS=10
