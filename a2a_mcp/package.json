{
  "name": "world-os-codex",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.7.1",
  "scripts": {
    "dev": "turbo run dev --parallel --filter=web --filter=api",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "build": "turbo run build",
    "chain:up": "anvil --host 0.0.0.0 --port 8545",
    "contracts:deploy": "pnpm --filter contracts deploy",
    "prepare": "husky install"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}
