apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vh2-dev
namePrefix: dev-

resources:
- ../../base

patches:
- target:
    kind: Deployment
    name: mcp-server
  patch: |-
    - op: replace
      path: /spec/replicas
      value: 1