{
  "name": "@world-os/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "@world-os/kernel": "workspace:*",
    "@world-os/sdk": "workspace:*",
    "bullmq": "^5.12.2",
    "ethers": "^6.13.1",
    "fastify": "^5.7.4",
    "@fastify/cors": "^11.2.0",
    "@fastify/websocket": "^11.2.0",
    "tslib": "^2.6.2",
    "viem": "^2.22.6",
    "@fastify/rate-limit": "^10.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.1",
    "eslint": "^8.57.0",
    "prisma": "^5.19.1",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
