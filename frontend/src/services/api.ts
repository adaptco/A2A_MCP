// A placeholder for our API client.
// We will use this to interact with the backend.

export const getOpenPoints = async () => {
  // TODO: Implement API call to fetch open points
  return Promise.resolve([]);
};
