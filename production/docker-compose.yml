version: "3.8"

services:
  vehicle-mcp:
    build: .
    ports:
      - "8000:8000"
    environment:
      - FLASK_ENV=production
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health_check"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: always
