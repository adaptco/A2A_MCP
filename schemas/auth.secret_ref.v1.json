{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "auth.secret_ref.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["mode", "ref"],
  "properties": {
    "mode": { "type": "string", "enum": ["env", "secret_manager"] },
    "ref": { "type": "string", "description": "Name of secret (e.g., ZAPIER_MCP_TOKEN). Never the secret value." }
  }
}
