# Agent: Codebase_Investigator

## Role & Objectives
The Codebase_Investigator is a specialized agentic helper focused on validating the implementation of the Core Orchestrator architecture. Its primary chore is to ensure that all walkthroughs, documentation, and build artifacts are correctly mapped and verifiable.

## Context from Gemini.md
- Project focusing on code agentic workflows.
- Researching new ways for agents to interact with the IDE.
- Preference for stable environments and persisted authorizations.
- **IDE Context**: Leverage `GEMINI_CLI_IDE_WORKSPACE_PATH` and native diffing for validation loops.

## Implementation Plan Review
- **A2A Agent Cards**: Successfully registered `orchestrator.core`, `manager.moa`, `coding.sentry`, and `chat.nlp`.
- **MoE Skills**: Integrated `spatial.3d`, `security.rbac`, `data.rag_lora`, and `microsoft-foundry.rbac`.
- **Project Structure**: Centered on the `core-orchestrator` repository with pnpm/turbo monorepo management.

## Task List for Validation
1. [x] **Registry Integrity**: Confirmed `agent_registry.json` and `expert_catalog.v1.json` are schema-valid.
2. [x] **Build Status**: Verified that `pnpm build` (turbo) passes across all 12 projects.
3. [x] **Walkthrough Audit**: `walkthrough.md` reflects the actual state of the registry and implementation plan.
4. [x] **Repo Mapping**: All paths align with the `core-orchestrator` workspace.

## Build & Mapping Status
- **Contracts**: Hardhat compile fixed with EVM `cancun` and OpenZeppelin 5.x overrides.
- **Web**: Vite build fixed with `type: module` and missing `ethers` dependency.
- **Kernel/SDK**: TS roots and JSON module resolution standardized.

---
*Generated by Agent Ralph Wiggum via Gemini CLI*
