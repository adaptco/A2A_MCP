# @world-os/kernel

Deterministic world-state reducer and physics helpers for World OS.

## Deployment package

```bash
npm --prefix packages/kernel run package:deploy
```

The command emits:

- `deploy/world-os-kernel-<version>.tgz`
- `deploy/manifest.json`
- `deploy/SHA256SUMS.txt`
